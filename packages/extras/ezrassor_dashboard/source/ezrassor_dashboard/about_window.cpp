// The AboutWindow class holds all functions required to show a small
// instructions window.
// Written by Tiger Sachse.
#include "about_window.h"

// Initialize this AboutWindow and setup the UI.
AboutWindow::AboutWindow(QWidget *parent) : QWidget(parent) {
    setupUi(this);
}
